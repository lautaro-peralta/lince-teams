faster-whisper>=1.0.0
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
psycopg[binary,pool]>=3.1   # pool: evita abrir una conexión TLS nueva por consulta
python-dotenv>=1.0    # carga .env en dev/Docker (no pisa el entorno de la plataforma)
